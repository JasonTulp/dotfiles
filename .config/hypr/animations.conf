animations {
  enabled = yes

  bezier = smooth, 0.05, 0.9, 0.1, 1.0
  bezier = snappy, 0.2, 0.9, 0.2, 1.0

  animation = windowsMove, 1, 2, snappy
  animation = windowsIn, 1, 3, smooth, slide
  animation = windowsOut, 1, 3, smooth, slide
  animation = fade, 1, 3, smooth
  animation = border, 1, 2, smooth
  animation = workspaces, 1, 4, smooth, slide
}

